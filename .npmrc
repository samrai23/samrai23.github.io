# Project-level .npmrc — overrides any global/user .npmrc
# Forces the public npm registry for this project so it builds
# correctly in CI environments that do not have access to private registries.
registry=https://registry.npmjs.org/
